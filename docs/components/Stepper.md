# Stepper

Sem componente Vue ainda. Existe apenas o arquivo de conexao de exemplo,
`src/components/Stepper.figma.ts.template`, gerado por `figma connect create` a partir
do component set real (`❖ Stepper`).

Props detectadas no Figma: `Title`, `Description`, `Show Chevron`, `Show Arrow`.

Para ativar: implementar `Stepper.vue`, ajustar o `example` do template para o markup
real e remover o sufixo `.template`.
