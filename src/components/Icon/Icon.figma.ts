import figma, { html } from '@figma/code-connect/html'

/* Componente `Icons/ System/ add-r` — Braip Components, node 663:44391.
 *
 * Cada icone do Figma e um COMPONENT avulso, entao o Code Connect e por icone,
 * apontando todos para o mesmo BraipIcon com o `name` correspondente. */
figma.connect('<FIGMA_LIBRARY>?node-id=663-44391', {
  example: () => html`<BraipIcon name="add-r" />`,
})
